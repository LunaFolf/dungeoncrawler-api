/**
 * Session Records are where game session data (progress, chat, etc) is stored.
 */
module.exports = {
  requiredDirectories: ['records'],
  init () {
    // Not needed atm, I think... but init is required so... yeah.
  }
}